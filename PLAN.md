# 大学 NPC 任务轴 · 施工计划

面向大学生的游戏化备考工作台 · 2026 微信小程序开发大赛参赛作品。

载体：微信小程序（个人主体 / 云开发）｜提报截止 **2026-10-17**｜今天 2026-09-24，剩 23 天

> **参赛决定（2026-09-24）**：本作品为提报项目，`ai-debate-arena` 不作为本次参赛提交
> （官方规定每人实名只能交一份作品）。

## 0. 交接状态（2026-09-25 14:47 UTC）

**已完成并推送**：https://github.com/Users54188/npc-task-axis （public / MIT / main）
`npm test` 177 个测试用例全绿，工作区与远端一致。

代码侧覆盖：知识库解析、时间轴与临期、倒计时、养成规则、服务端校验、提醒计划、
双向同步合并、数据层端到端、页面生命周期、真实 WXML 模板渲染、配置自检、跨设备旅程。
`npm run preview` 可用真实模板+真实数据生成 HTML 预览供视觉核对。
`npm run pdf` 用无头 Chrome 把说明文档渲染成 A4 PDF（提报必需），同时列出未填占位项。

**未完成，且必须你本人操作**：

| 项 | 位置 | 卡在哪 |
|---|---|---|
| 提交小程序备案 | 后台 → 设置 → 基本设置 → 小程序备案 | 管局审核 1–20 工作日，距 10/17 仅 23 天，**最高风险** |
| `CLOUD_ENV` | `miniprogram/utils/config.js` | 需先开通云开发拿到环境 ID |
| `SUBSCRIBE_TMPL_IDS` | 同上 | 需先在后台申请订阅消息模板 |
| `REMIND_CONFIG` | 云函数 `remind` 环境变量 | 依赖上一项的模板 ID 与字段名 |
| 真机运行 / 云函数部署 / 提审 | 微信开发者工具与后台 | 本环境无开发者工具，代码从未真机跑过 |

**已知工具限制**：Computer Use 在本机无法驱动 Edge（`browser_url_policy` 四次在第一步即拒绝，
且不允许恢复窗口），因此后台页面无法自动读取；Edge 窗口目前处于最小化，需手动恢复。

**恢复推进的方式**：拿到上面三个值后交给 agent 即可填配置并推第二版；
或在 `/goal resume` 后继续。功能层面已无需要新写的模块。

## 0. 已确认的外部约束（不是假设，是查到的规则）

| 约束 | 来源 | 影响 |
|---|---|---|
| 作品须在 7/17–10/17 期间在微信公众平台**新上线**且能正常运行 | 官方赛事规程 | 备案与提审是关键路径，功能必须收窄 |
| 提交物含 AppID、小程序码、说明文档 PDF、授权书 | 官方赛事规程 | 上线拿不到版本号 = 无法提报 |
| 每人限交一份作品，≤3 人组队 | 官方赛事规程 | — |
| 个人主体**拿不到深度合成类目**（算法备案在用证明仅非个人主体可申请） | 云开发在用证明文档 | 运行时不得对用户开放生成式能力 |
| 个人主体**不支持 `web-view`** | 小程序平台规则 | 「报名网站」只能复制，不能内嵌打开 |
| 工具类目基本只有一**次性**订阅消息 | 小程序平台规则 | 提醒需每次打卡时顺手续订 |
| 成长计划二期 10 亿 token（`hy3-preview`），自申请起 6 个月有效，一账号一次 | 小程序成长计划文档 | 免费算力，但不改类目限制 |

## 1. 产品定义

面向大学生的游戏化备考工作台。两张在线表 + 一个内置知识库，把"证书考试 + 专业赛事"的**报名 / 准考证 / 考试 / 查分**四类节点排成一条任务轴，按日期高亮临期项，已报名的任务生成考试倒计时，再用阶段打卡 + 经验等级把备考过程游戏化。

**AI 的合法落点（三条，全部避开生成式）**
1. **知识库匹配**：输入任务名 → 命中内置证书/赛事库 → 自动回填常规时间窗口与阶段计划。检索，非生成。
2. **报名表识别**（第二阶段）：拍照 → OCR/结构化 → 填入任务节点。判别，非生成。
3. **离线内容管线**：知识库本身由 AI 辅助整理，线上只读静态数据。

**明确不做**：AI 对话、AI 生图、AI 文案润色、桌宠实时互动、PC 多栏布局。

## 2. 数据模型

`taskNodes`（任务节点）：`name` `shortName` `type(certificate|contest)` `site` `place` `fee` `regStart/regEnd` `admitStart/admitEnd` `examDate` `scoreDate` `registered` `plan[]` `_openid` `createdAt` `updatedAt`

`checkIns`（打卡记录）：`nodeId` `date(YYYY-MM-DD)` `subject` `stage` `note` `focusMinutes` `tags[]` `makeup` `_openid` `updatedAt`

同步策略（已实现于 `utils/sync.js` + `utils/store.js`，23 个测试用例覆盖）：
本地写 → 云开发落库 → `watch` 实时回推。四条规则：
1. 有未 flush 操作的行**远端不得覆盖**（离线编辑保护）；
2. 其余按 `updatedAt` 后者胜，合并时保留本地独有字段；
3. 远端 `remove`/`dequeue` 与 pull 缺失行都按删除处理，但受第 1 条保护；
4. 未同步成功的本地行没有云端主键，用**业务键**（任务 `name|examDate`、打卡 `nodeId|date|subject`）
   把 watch/pull 回包认回同一行，避免插成重复；非脏行不做业务键猜测。
`pull` 返回 `null`（请求失败）时不清空本地。

## 3. 页面

| 页 | 路径 | 内容 |
|---|---|---|
| 时间轴（首页） | `pages/timeline/` | 四类节点竖排时间轴、临期红底高亮、通用证书/专业赛事分段、顶部统计、章节进度 |
| 任务详情 | `pages/task-detail/` | 倒计时、四节点、报名信息（复制链接）、阶段计划、已报名切换、编辑删除 |
| 阶段打卡 | `pages/checkin/` | 月历 + 补卡、科目与今日重点、完成率、打卡 +EXP |
| 专注 | `pages/focus/` | 25/5/15 番茄钟（**时间戳基准**）、学习标签、专注统计 |
| 我的 | `pages/mine/` | 经验等级、连续天数、徽章、桌宠（预设台词）、数据导出 |

## 4. 排期（关键路径 = 备案）

- **9/24 今天**：注册小程序拿 AppID → **提交备案** → 领 10 亿 token → 定类目（教育>教育信息展示，副 工具>备忘录；效率不对个人开放）。代码用 `touristappid` 并行开工，不等 AppID。
- **9/25–10/1**：知识库数据 + 时间轴/详情/打卡三页 + 云开发两表 CRUD。
- **10/2–10/8**：云函数 + 双向同步 + 订阅消息 + 30–50 人体验版内测攒数据。
- **10/9–10/13**：canvas 任务轴长图分享 + **正式提审**（预留 2–3 轮）。
- **10/14–10/16**：发布拿版本号 → 说明文档 PDF + 授权书 + 小程序码。
- **10/17**：提报。

备案兜底：功能开发不受备案阻塞（体验版可跑），若 10/10 备案未回，材料里附受理编号 + 录屏，并邮件 weixindeveloper@tencent.com 说明赛区与截止冲突。

## 5. 当前阻塞

需要后台页面信息（Computer Use 在本机被 URL 策略拒绝，无法自行读取）：
主体类型、AppID、微信认证状态、**备案状态**、已选服务类目、云开发环境与配额、服务器域名白名单、订阅消息可用模板。
